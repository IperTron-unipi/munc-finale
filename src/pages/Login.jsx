function Login() {
  // TODO: modalità del form — 'accedi' | 'registrati'
  // TODO: campi email e password, stato di errore, stato di invio
  // TODO: submit → signInWithEmailAndPassword / createUserWithEmailAndPassword
  // TODO: se l'utente è già autenticato, redirect a "/"

  return (
    <section>
      {/* Form email + password, con interruttore fra "Accedi" e "Registrati" */}
      {/* Messaggio d'errore: email-already-in-use, invalid-email, weak-password, invalid-credential */}
    </section>
  )
}

export default Login
