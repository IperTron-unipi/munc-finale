function CreateGame() {
  return (
    <section>
      {/* Form nome personaggio. Crea games/{gameId} (status: lobby) + player. Redirect a /game/:gameId */}
    </section>
  )
}

export default CreateGame
