function Lobby({ gameId }) {
  return (
    <section>
      <p>Partita {gameId}</p>
      {/* Lista giocatori realtime, codice da condividere, bottone "Inizia" (solo host) */}
    </section>
  )
}

export default Lobby
