function Playing({ gameId }) {
  return (
    <section>
      {/* Board con tutti i personaggi (livello, flag Combattente), proprio personaggio editabile */}
    </section>
  )
}

export default Playing
